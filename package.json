{
  "name": "cherrytor-monorepo",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "pnpm -r build",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "test": "node --experimental-strip-types --test tests/unit/*.test.ts tests/integration/*.test.ts tests/security/*.test.ts"
  },
  "devDependencies": {
    "@types/node": "^26.2.0",
    "typescript": "^5.5.4"
  }
}
